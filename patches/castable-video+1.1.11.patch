diff --git a/node_modules/castable-video/castable-mixin.js b/node_modules/castable-video/castable-mixin.js
index 26415cf..a4c9407 100644
--- a/node_modules/castable-video/castable-mixin.js
+++ b/node_modules/castable-video/castable-mixin.js
@@ -55,6 +55,7 @@ export const CastableMediaMixin = (superclass) =>
     }
 
     get #castPlayer() {
+      if (this.disableRemotePlayback) return undefined;
       return privateProps.get(this.remote)?.getCastPlayer?.();
     }
 
@@ -66,6 +67,12 @@ export const CastableMediaMixin = (superclass) =>
         return;
       }
 
+      // Only cast-src / cast-stream-type need to reload cast media; do not touch `#castPlayer`
+      // for unrelated attrs (e.g. playback-id, src) — it forces `remote` and loads cast_sender.js.
+      if (attrName !== 'cast-stream-type' && attrName !== 'cast-src') return;
+
+      if (this.disableRemotePlayback) return;
+
       if (!this.#castPlayer) return;
 
       switch (attrName) {
